apiVersion: v1
kind: Service
metadata:
  name: lab4-svc
  namespace: lab4
spec:
  type: NodePort
  selector:
    app: lab4-pod
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30084

